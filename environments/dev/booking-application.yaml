# gitops-platform/environments/dev/booking-application.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: booking-service
  annotations:
    environment: dev
spec:
  project: default
  source:
    repoURL: placeholder-repo  # Will be replaced by kustomize patch
    targetRevision: placeholder-rev  # Will be replaced by kustomize patch
    path: services/booking-service/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: booking-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m