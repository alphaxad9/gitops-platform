# infrastructure/base/kafka/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kafka-operator

resources:
  - namespace.yaml
  - operator.yaml
  - kafka-nodepool.yaml
  - kafka-cluster.yaml

# ⚠️ DO NOT add patchesStrategicMerge or patchesJson6902 that inject STRIMZI_KAFKA_IMAGES
# Let Strimzi use its built-in, validated Kafka version catalog