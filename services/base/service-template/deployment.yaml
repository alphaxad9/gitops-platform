# gitops-platform/services/base/service-template/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-placeholder
  labels:
    app: service-placeholder
    service: service-placeholder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-placeholder
  template:
    metadata:
      labels:
        app: service-placeholder
        service: service-placeholder
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: service-placeholder-sa
      containers:
      - name: service-placeholder
        image: placeholder:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
          name: http
        - containerPort: 8001
          name: metrics
        envFrom:
        - configMapRef:
            name: service-placeholder-config
        - secretRef:
            name: service-placeholder-secrets
        - secretRef:
            name: postgres-credentials
        - secretRef:
            name: rabbitmq-credentials
        - secretRef:
            name: kafka-credentials
        - secretRef:
            name: redis-credentials
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health/
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/
            port: http
          initialDelaySeconds: 20
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        startupProbe:
          httpGet:
            path: /health/
            port: http
          failureThreshold: 30
          periodSeconds: 10
          timeoutSeconds: 5
        volumeMounts:
        - name: static-files
          mountPath: /app/static
        - name: media-files
          mountPath: /app/media
        - name: keys
          mountPath: /app/keys
          readOnly: true
      volumes:
      - name: static-files
        persistentVolumeClaim:
          claimName: service-placeholder-static-pvc
      - name: media-files
        persistentVolumeClaim:
          claimName: service-placeholder-media-pvc
      - name: keys
        secret:
          secretName: service-placeholder-secrets
          items:
          - key: SIGNING_KEY
            path: private.pem
      - name: tmp
        emptyDir: {}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000