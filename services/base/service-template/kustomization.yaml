# gitops-platform/services/base/service-template/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml

labels:
  - pairs:
      managed-by: gitops
      part-of: microservices-platform
    includeSelectors: false