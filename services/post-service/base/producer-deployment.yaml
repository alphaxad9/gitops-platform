# services/post-service/base/producer-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-producer
  labels:
    app: post-service
    component: producer
    managed-by: gitops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: post-service
      component: producer
  template:
    metadata:
      labels:
        app: post-service
        component: producer
    spec:
      serviceAccountName: post-service-sa
      containers:
      - name: post-producer
        image: alphaxad9/post_service:0.0.5
        imagePullPolicy: IfNotPresent
        command: ["./bin/outbox-publisher"]
        envFrom:
        - configMapRef:
            name: post-config
        - secretRef:
            name: post-secrets
        - secretRef:
            name: postgres-credentials
        - secretRef:
            name: kafka-credentials
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: SERVICE_COMPONENT
          value: "kafka-producer"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000